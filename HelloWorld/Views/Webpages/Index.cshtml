@model IEnumerable<HelloWorld.Models.Webpage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.URLAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.URLAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comment)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WebPageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WebPageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WebPageID })
        </td>
    </tr>
}

</table>
